#include <iostream> 
#include <string>
#include "Task 2_lab4.h"
#include <math.h>
using namespace std;


/*int main()
{
	Point2D p1;
	p1.x = 5;
	p1.y = 3;
	Point2D p2;
	p2.x = 10;
	p2.y = 1;
	float distance =p1.distance(p2.x,p2.y);
	cout << "the distance between p1 and p2 is " << distance << endl;
	Point2D newPoint = p2;
	cout << "´the new point are " <<newPoint.toString() << endl;
	Point2D newPoint2;
	newPoint2= p1 + p2;
	cout << "the new object which is created from p1 and p2 by adding is " << newPoint2.toString()<< endl;
	float distance1 =p1.distance(newPoint2.x,newPoint2.y); 
	cout << "the distance between p1 and p2 is " << distance1 << endl;
	cout << "To compare:\n the new point is " << newPoint2.toString() << "And the other points \n" << p1.toString() << "\n" << p2.toString() << "\n" << newPoint.toString() << endl;

	
}*/